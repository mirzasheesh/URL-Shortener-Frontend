import Styles from '../styles.module.css';

export default function Section() {
    return (
        <div className = {Styles.section}>
            <p>Shortify is a free tool to short a long URL or reduce the length of a web page link</p>
            <p>Use our URL Shortener to create a shortened link and makes it easy to remember</p>
        </div>
    );
}